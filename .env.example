# Shared VMDB Supabase project for SocialMarket and dependent portfolio apps.
# Browser/public configuration is pinned in code to the canonical VMDB project.
NEXT_PUBLIC_SUPABASE_URL=https://gqpbskssrvpfjtujwezc.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_Kcat2PHVjGn32ubiefotfA_iJjCU-B2

# Optional privileged server persistence. The orchestrator can instead use the
# signed-in approved admin JWT through RLS, so this is not mandatory on Vercel.
SUPABASE_SERVICE_ROLE_KEY=

# Optional Vercel/server model providers. GitHub coding tasks use GitHub Models
# with the workflow GITHUB_TOKEN and do not require another paid model key.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=
OPENROUTER_API_KEY=
OPENROUTER_FALLBACK_MODEL=
ADMIN_EMAIL=
DRIVE_FILE_ID=

# Optional system-to-system auth. Interactive admin calls may authenticate with
# the approved Supabase admin access token instead.
ORCHESTRATOR_SECRET=
ORCHESTRATOR_MAX_PROMPT_CHARS=6000
AGENT_MAX_OUTPUT_TOKENS=900
AGENT_AI_TIMEOUT_MS=20000

# Research infrastructure
SEARXNG_BASE_URL=
CRAWL4AI_BASE_URL=

# IMPORTANT OWNERSHIP BOUNDARY
# BUFFER_API_KEY, Meta/TikTok publishing credentials and execution-provider
# credentials belong to the SocialScheduler runtime, not SocialMarket.
