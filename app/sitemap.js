import{inventory,SITE}from'../lib/affinity';import{industries,solutions}from'../lib/taxonomy';export default async function sitemap(){const items=(await inventory(42)).filter(x=>x.verification?.status!=='AI_SCREENING');const now=new Date();return[{url:SITE,lastModified:now,changeFrequency:'daily',priority:1},...Object.keys(industries).map(slug=>({url:`${SITE}/industries/${slug}`,lastModified:now,changeFrequency:'weekly',priority:.8})),...Object.keys(solutions).map(slug=>({url:`${SITE}/solutions/${slug}`,lastModified:now,changeFrequency:'weekly',priority:.8})),...items.map(x=>({url:`${SITE}/products/${x.aliexpressId}`,lastModified:new Date(x.verification?.checked||x.updatedAt||Date.now()),changeFrequency:'weekly',priority:.75}))]}