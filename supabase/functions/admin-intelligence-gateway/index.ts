import 'jsr:@supabase/functions-js/edge-runtime.d.ts'
import { createClient } from 'npm:@supabase/supabase-js@2.57.4'
const ADMIN='vmoulakakis@gmail.com',DEEPSEEK_KEY=Deno.env.get('DEEPSEEK_API_KEY')||Deno.env.get('DEEP_SEEK_API_KEY')||'',MODEL=Deno.env.get('DEEPSEEK_MODEL')||'deepseek-v4-pro',SUPABASE_URL=Deno.env.get('SUPABASE_URL')!,ANON_KEY=Deno.env.get('SUPABASE_ANON_KEY')!
const json=(x:unknown,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}})
function decodeJwt(token:string){const part=token.split('.')[1];if(!part)throw new Error('invalid_jwt');const normalized=part.replace(/-/g,'+').replace(/_/g,'/'),pad='='.repeat((4-normalized.length%4)%4);return JSON.parse(atob(normalized+pad))}
async function deepseek(system:string,input:unknown,maxTokens=2500){if(!DEEPSEEK_KEY)throw new Error('deepseek_not_configured');const r=await fetch('https://api.deepseek.com/chat/completions',{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${DEEPSEEK_KEY}`},body:JSON.stringify({model:MODEL,temperature:.12,max_tokens:maxTokens,response_format:{type:'json_object'},messages:[{role:'system',content:system},{role:'user',content:JSON.stringify(input)}]})});const raw=await r.text();if(!r.ok)throw new Error(`deepseek_${r.status}:${raw.slice(0,500)}`);const j=JSON.parse(raw);return{data:JSON.parse(String(j?.choices?.[0]?.message?.content||'{}')),usage:j?.usage||{}}}
Deno.serve(async req=>{if(req.method==='GET')return json({ok:true,service:'admin-intelligence-gateway',version:'7.0',model:MODEL,deepseek_configured:Boolean(DEEPSEEK_KEY)});if(req.method!=='POST')return json({error:'method_not_allowed'},405);try{const auth=req.headers.get('authorization')||'';if(!auth.startsWith('Bearer '))throw new Error('missing_auth');const token=auth.slice(7),claims=decodeJwt(token);if(String(claims.email||'').toLowerCase()!==ADMIN)throw new Error('admin_only');const sb=createClient(SUPABASE_URL,ANON_KEY,{global:{headers:{Authorization:`Bearer ${token}`}}}),body=await req.json(),action=String(body.action||'')
if(action==='product_profile'){const p=body.product||{};if(!p.product_id||!p.offer_id||!p.tracking_url)throw new Error('validated_product_required');const system=`You are SocialMarket Elite Affiliate Product Strategist. Use ONLY supplied validated product facts, semantic taxonomy, network merchant-program KPIs and validated pain/demand/competition evidence. Never invent product features, reviews, search volume, CPC, CTR, social reach, scarcity, sales, AOV or conversion history. Network CVR/EPC/approval are OBSERVED NETWORK BASELINES, not SocialMarket first-party performance. Produce strict JSON: {semantic_keywords:[max 18],low_competition_tags:[max 15],promotion_angle,promotion_description,channel_recommendation:{primary_channel,secondary_channels,organic_vs_paid_hypothesis,why,missing_data},funnel_recommendation:{awareness,consideration,conversion,retargeting,cta},conversion_scenarios_per_100_clicks:{conservative:{basis,cvr_pct,approved_conversions,modeled_commission_eur},base:{basis,cvr_pct,approved_conversions,modeled_commission_eur},upside:{basis,cvr_pct,approved_conversions,modeled_commission_eur}},forecast_7d:{direction,confidence,what_to_test,kpis},forecast_30d:{direction,confidence,what_to_test,kpis},forecast_90d:{direction,confidence,what_to_test,kpis},forecast_confidence,claims_audit:{supported_claims,avoided_claims,evidence_limits},unsupported_claims:[]}. Semantic keywords must describe actual product/validated pain intent. Low-competition tags are hypotheses derived from specific long-tail semantics and supplied competition evidence; never claim measured keyword difficulty unless supplied. Promotional description must be unique, persuasive Greek copy but factual. Conversion scenarios must be transparent transformations of supplied CVR/approval/commission, not made-up traffic forecasts.`;const r=await deepseek(system,p,4200);const {data:id,error}=await sb.rpc('admin_save_product_marketing_profile',{p_product_id:p.product_id,p_offer_id:p.offer_id,p_profile:r.data,p_provider:'deepseek',p_model:MODEL});if(error)throw error;await sb.rpc('admin_ai_log',{p_task_type:'product_marketing_profile',p_provider:'deepseek',p_model_name:MODEL,p_status:'success',p_input_tokens:Number(r.usage?.prompt_tokens||0),p_output_tokens:Number(r.usage?.completion_tokens||0),p_metadata:{product_id:p.product_id,profile_id:id}});return json({ok:true,model:MODEL,profile_id:id,profile:r.data,usage:r.usage})}
if(action==='optimization_brief'){const o=body.optimization||{},simulation=body.simulation||null,source={generated_at:o.generated_at,data_coverage:o.data_coverage||{},network_kpi_summary:o.network_kpi_summary||{},reaction_queue:(o.reaction_queue||[]).slice(0,80),program_performance:(o.program_performance||[]).slice(0,120),product_profitability:(o.product_profitability||[]).slice(0,80),first_party_30d:o.first_party_30d||[],first_party_totals_30d:o.first_party_totals_30d||{},channel_evidence:o.channel_evidence||[],publishing_by_platform:o.publishing_by_platform||[],methodology:o.methodology||{},simulation};const system=`You are SocialMarket 360 Affiliate Optimization AI. Use ONLY supplied production metrics and explicit simulation assumptions. Never invent CTR,CPC,CPM,traffic,conversions,revenue,AOV,market size,search volume,social reach,product performance or channel performance. Network CVR/EPC/approval are observed baselines, not first-party results. If first-party history is zero, no channel is performance-proven. Produce strict JSON: {executive_decision,confidence_0_100,data_truth:{observed,estimated,missing},best_opportunities:[{merchant,program,signal,why,observed_metrics,confidence_0_100,reaction}],funnel_strategy:[{priority,channel,funnel,objective,why,observed_support,missing_data,confidence_0_100}],ab_tests:[{hypothesis,variant_a,variant_b,primary_kpi,secondary_kpis,guardrails,decision_rule,why_now}],profitability_insights:[{entity,insight,evidence,action}],conversion_forecast:{basis,per_100_clicks_scenarios,limits},quick_reactions:[{trigger,action,why}],next_7_days,next_30_days}`;const r=await deepseek(system,source,5000);await sb.rpc('admin_ai_log',{p_task_type:'affiliate_optimization',p_provider:'deepseek',p_model_name:MODEL,p_status:'success',p_input_tokens:Number(r.usage?.prompt_tokens||0),p_output_tokens:Number(r.usage?.completion_tokens||0),p_metadata:{first_party_rows:Number(o?.data_coverage?.first_party_metric_rows_30d||0)}});return json({ok:true,model:MODEL,optimization:r.data,usage:r.usage})}
if(action==='business_brief'){const bi=body.bi||{},source={generated_at:bi.generated_at,pipeline:bi.pipeline||{},freshness:bi.freshness||{},queue_alerts:bi.queue_alerts||{},ai_summary:bi.ai_summary||{},audit_distribution:(bi.audit_distribution||[]).slice(0,10),evidence_by_source:(bi.evidence_by_source||[]).slice(0,20),evidence_by_platform:(bi.evidence_by_platform||[]).slice(0,20),top_pain_clusters:(bi.top_pain_clusters||[]).slice(0,30),merchant_opportunities:(bi.merchant_opportunities||[]).slice(0,30),product_opportunities:(bi.product_opportunities||[]).slice(0,30),research_job_health:(bi.research_job_health||[]).slice(0,40),collection_job_health:(bi.collection_job_health||[]).slice(0,40),merchant_runs:(bi.merchant_runs||[]).slice(0,20),product_config:bi.product_config||null};const system=`You are SocialMarket AI Business Intelligence Analyst. Analyze ONLY supplied production metrics. Never invent revenue,traffic,search volume,conversions,market share,users,products,costs or trends. Missing data must be explicit. Separate observed facts from inference. Produce strict JSON: {executive_summary,data_health:{score_0_100,issues,freshness_comment},pipeline_health:{score_0_100,bottlenecks,conversion_observations},opportunity_insights:[{title,why_it_matters,evidence,confidence_0_100}],operational_risks:[{risk,severity,action}],ai_efficiency:{assessment,actions},recommended_actions:[{priority,action,reason,expected_effect,metric_to_watch}],questions_to_answer_next}.`;const r=await deepseek(system,source,4200);await sb.rpc('admin_ai_log',{p_task_type:'business_brief',p_provider:'deepseek',p_model_name:MODEL,p_status:'success',p_input_tokens:Number(r.usage?.prompt_tokens||0),p_output_tokens:Number(r.usage?.completion_tokens||0),p_metadata:{generated_at:bi.generated_at||null}});return json({ok:true,model:MODEL,brief:r.data,usage:r.usage})}
if(action==='forecast'){const source={generated_at:body.generated_at,category_market:(body.category_market||[]).filter((x:any)=>x?.methodology_version==='semantic_taxonomy_v1').slice(0,120),pain_gaps:(body.pain_gaps||[]).filter((x:any)=>x?.validation_status==='validated').slice(0,80),merchants:(body.merchants||[]).slice(0,80),social:(body.social||[]).slice(0,100)};const system=`You are SocialMarket Forecast Analyst for the Greek affiliate market. Use ONLY supplied semantic-taxonomy production evidence. Never invent volumes,market shares,social metrics or dates. If validated pains are absent, say so and lower confidence. Produce JSON: {as_of,horizon_months:3,market_outlook,confidence_0_100,drivers:[{name,direction,confidence,evidence}],category_forecasts:[{category,current_signal,month_1,month_2,month_3,confidence,why}],risks,affiliate_actions,methodology_note}. Scores are directional indices, not fabricated volumes.`;const r=await deepseek(system,source,3200);await sb.rpc('admin_ai_log',{p_task_type:'forecast',p_provider:'deepseek',p_model_name:MODEL,p_status:'success',p_input_tokens:Number(r.usage?.prompt_tokens||0),p_output_tokens:Number(r.usage?.completion_tokens||0),p_metadata:{horizon_months:3}});return json({ok:true,model:MODEL,forecast:r.data,usage:r.usage})}
if(action==='promo'){const product=body.product||{},pain=body.pain||null,platforms=Array.isArray(body.platforms)?body.platforms:['facebook','instagram','tiktok','linkedin'];if(!product.tracking_url)throw new Error('tracking_url_required');const system=`You are SocialMarket Affiliate Creative Agent. Use ONLY supplied product,merchant,validated pain and commercial facts. Do not invent features,reviews,scarcity,discounts or performance claims. Keep exact tracking_url. Produce JSON: {product_card:{headline,subheadline,bullets,cta,disclaimer},image_prompt,posts:{facebook:{copy,hashtags},instagram:{caption,hashtags},tiktok:{hook,script,caption,hashtags},linkedin:{copy,hashtags}},audit:{claims_used,unsupported_claims_avoided,evidence_note}}.`;const r=await deepseek(system,{product,pain,platforms},3600);await sb.rpc('admin_ai_log',{p_task_type:'promo',p_provider:'deepseek',p_model_name:MODEL,p_status:'success',p_input_tokens:Number(r.usage?.prompt_tokens||0),p_output_tokens:Number(r.usage?.completion_tokens||0),p_metadata:{product_id:product.product_id||null,platforms}});return json({ok:true,model:MODEL,promo:r.data,usage:r.usage})}
throw new Error('action_not_allowed')}catch(e){const msg=String(e instanceof Error?e.message:e);return json({error:msg},msg.includes('admin_only')?403:400)}})
