import 'jsr:@supabase/functions-js/edge-runtime.d.ts'
import { createRemoteJWKSet, jwtVerify } from 'npm:jose@6.1.0'
import postgres from 'https://deno.land/x/postgresjs@v3.4.5/mod.js'
const sql=postgres(Deno.env.get('SUPABASE_DB_URL')!,{prepare:false,max:1})
const ISSUER='https://token.actions.githubusercontent.com',AUDIENCE='socialmarket-supabase-worker',REPOSITORY_ID='1329707883',REPOSITORY='vmoulakakis/Socialmarket'
const ALLOWED=new Set(['vmoulakakis/Socialmarket/.github/workflows/generic-evidence-intelligence.yml@refs/heads/main'])
const JWKS=createRemoteJWKSet(new URL(`${ISSUER}/.well-known/jwks`))
const DEEPSEEK_KEY=Deno.env.get('DEEPSEEK_API_KEY')||Deno.env.get('DEEP_SEEK_API_KEY')||'',MODEL=Deno.env.get('DEEPSEEK_MODEL')||'deepseek-v4-pro'
const clamp=(n:number,a=0,b=100)=>Math.max(a,Math.min(b,n)),json=(x:unknown,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}}),domain=(u:string|null)=>{try{return new URL(u||'').hostname.toLowerCase().replace(/^www\./,'')}catch{return ''}}
async function auth(req:Request){const h=req.headers.get('authorization')||'';if(!h.startsWith('Bearer '))throw new Error('missing_oidc');const{payload}=await jwtVerify(h.slice(7),JWKS,{issuer:ISSUER,audience:AUDIENCE});if(String(payload.repository_id||'')!==REPOSITORY_ID||String(payload.repository||'')!==REPOSITORY||String(payload.ref||'')!=='refs/heads/main'||!ALLOWED.has(String(payload.workflow_ref||'')))throw new Error('oidc_not_allowed');return payload}
async function taxonomy(id:string){const r=await sql`select t.id,t.name,t.node_type,t.parent_id,p.name parent_name from catalog.taxonomy_nodes t left join catalog.taxonomy_nodes p on p.id=t.parent_id where t.id=${id} and t.semantic_status='validated' and t.active=true and(p.id is null or p.semantic_status='validated') limit 1`;if(!r[0])throw new Error('canonical_taxonomy_required');return r[0]}
async function seed(){const r=await sql`insert into ops.collection_jobs(entity_type,entity_id,collection_type,status,priority,reason,collector_policy,requested_at,not_before,dedupe_key,payload) select 'taxonomy',t.id,'semantic_category_pain_v2','queued',case when t.node_type='subcategory' then 75 else 60 end,'semantic_greek_market_pain_v3',${sql.json({collector:'searxng',retrieval:'greek_natural_alias_v3+authoritative_context_v1',ai_audit:'deepseek',taxonomy_policy:'closed_semantic_product_taxonomy_v1'})},now(),now(),'semantic-pain-v3:'||t.id::text||':'||to_char((now() at time zone 'Europe/Athens')::date,'YYYY-MM-DD'),jsonb_build_object('name',t.name,'node_type',t.node_type,'category',case when t.node_type='subcategory' then p.name else t.name end,'subcategory',case when t.node_type='subcategory' then t.name else null end,'geography','GR','semantic_status','validated','retrieval_version','greek_alias_v3+authoritative_context_v1') from catalog.taxonomy_nodes t left join catalog.taxonomy_nodes p on p.id=t.parent_id where t.semantic_status='validated' and t.active=true and t.node_type in('category','subcategory') on conflict do nothing returning id`;return{seeded:r.length,collection_type:'semantic_category_pain_v2',retrieval:'greek_alias_v3+authoritative_context_v1'}}
async function claim(limit:number,worker:string){return await sql`with picked as(select id from ops.collection_jobs where status='queued' and not_before<=now() and collection_type='semantic_category_pain_v2' order by priority desc,requested_at asc for update skip locked limit ${Math.max(1,Math.min(limit,30))}),upd as(update ops.collection_jobs j set status='running',lease_owner=${worker},lease_expires_at=now()+interval '60 minutes',attempt_count=attempt_count+1 from picked p where j.id=p.id returning j.*) select * from upd`}
async function deepseekBatch(items:any[]){if(!DEEPSEEK_KEY)throw new Error('deepseek_not_configured');const system=`You are SocialMarket Greek Consumer Pain Skeptic. Extract ONLY product-related consumer problems, unmet needs, alternative requests or purchase frictions actually supported by evidence for the EXACT canonical category/subcategory. Reject news topics, general social problems, merchant/company issues unrelated to product need, navigation text, locations, brands, campaign themes and keyword coincidences. Evidence rows are indexed from 0. A cluster may be proposed only if its evidence_indices point to relevant rows. Prefer recurring desired outcomes and solvable frictions. Do not invent search volume, competition, features, prices or popularity. Return strict JSON {items:[{entity_id,clusters:[{canonical_text,cluster_type:'pain'|'unmet_need'|'alternative_request'|'complaint',evidence_indices:[...],pain_severity:0-100,commercial_intent:0-100,audit_score:0-100,confidence:0-1,rationale,verdict:'validated'|'needs_review'|'rejected'}],audit_summary,rejected_patterns:[...]}]}. Use validated only when at least 3 relevant evidence rows from at least 2 independent source domains support a commercially meaningful product need.`;const payload={items:items.slice(0,6).map(x=>({entity_id:x.entity_id,category:x.category,subcategory:x.subcategory,evidence:(x.evidence||[]).filter((e:any)=>e.source_kind==='pain_candidate').slice(0,50).map((e:any,i:number)=>({i,source_url:e.source_url,title:e.title,body:String(e.body||'').slice(0,900),confidence:e.confidence}))}))};const r=await fetch('https://api.deepseek.com/chat/completions',{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${DEEPSEEK_KEY}`},body:JSON.stringify({model:MODEL,temperature:.05,max_tokens:5200,response_format:{type:'json_object'},messages:[{role:'system',content:system},{role:'user',content:JSON.stringify(payload)}]})}),raw=await r.text();if(!r.ok)throw new Error(`deepseek_${r.status}:${raw.slice(0,500)}`);const j=JSON.parse(raw),out=JSON.parse(String(j?.choices?.[0]?.message?.content||'{}'));return{items:Array.isArray(out.items)?out.items:[],model:MODEL,usage:j?.usage||{}}}
async function persistEvidence(entityId:string,items:any[]){let n=0;for(const e of items.slice(0,240)){const kind=String(e.source_kind||'web_observation').slice(0,80),url=e.source_url||null,body=e.body||null,title=e.title||null,platform=e.platform||null,collector=String(e.collector||'semantic_category_pain_v3').slice(0,80),confidence=clamp(Number(e.confidence??.6),0,1),hash=String(e.content_hash||await crypto.subtle.digest('SHA-256',new TextEncoder().encode(`${kind}|${url||''}|${title||''}|${body||''}`)).then(b=>Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,'0')).join(''))),retrievalVersion=String(e?.metadata?.retrieval_version||'greek_alias_v3');await sql`insert into evidence.observations(entity_type,entity_id,source_kind,platform,source_url,source_domain,collector,title,body,metrics,metadata,confidence,validation_status,content_hash) values('taxonomy',${entityId},${kind},${platform},${url},${domain(url)||null},${collector},${title},${body},${sql.json(e.metrics||{})},${sql.json({...e.metadata,metric_semantics:'observed_source_evidence',retrieval_version:retrievalVersion})},${confidence},${confidence>=.72?'validated':'pending'},${hash}) on conflict(entity_type,entity_id,source_kind,content_hash) do update set collected_at=now(),confidence=greatest(evidence.observations.confidence,excluded.confidence),metadata=evidence.observations.metadata||excluded.metadata`;n++}return n}
async function save(result:any){const jobId=String(result.job_id||''),entityId=String(result.entity_id||'');if(!jobId||!entityId)throw new Error('missing_ids');const tx=await taxonomy(entityId),category=tx.node_type==='subcategory'?tx.parent_name:tx.name,subcategory=tx.node_type==='subcategory'?tx.name:null,evidence=Array.isArray(result.evidence)?result.evidence:[],persisted=await persistEvidence(entityId,evidence),painEvidence=evidence.filter((e:any)=>e.source_kind==='pain_candidate');let validated=0,total=0;const painScores:number[]=[];for(const c of(Array.isArray(result.clusters)?result.clusters:[]).slice(0,60)){const text=String(c.canonical_text||'').trim();if(text.length<20)continue;const rawIndices:number[]=(Array.isArray(c.evidence_indices)?c.evidence_indices:[]).map((x:any)=>Number(x)).filter((x:number)=>Number.isInteger(x)&&x>=0&&x<painEvidence.length);const idx:number[]=[...new Set<number>(rawIndices)],refs=idx.map(i=>painEvidence[i]),domains=new Set(refs.map((e:any)=>domain(e?.source_url||null)).filter(Boolean)),conf=clamp(Number(c.confidence??0),0,1),audit=clamp(Number(c.audit_score??0)),severity=clamp(Number(c.pain_severity??0)),intent=clamp(Number(c.commercial_intent??0)),verdict=String(c.verdict||'needs_review'),status=verdict==='validated'&&idx.length>=3&&domains.size>=2&&conf>=.72&&audit>=72&&severity>=50&&intent>=35?'validated':'pending',clusterType=['pain','unmet_need','alternative_request','complaint'].includes(String(c.cluster_type))?String(c.cluster_type):'pain';await sql`insert into evidence.semantic_clusters(entity_type,entity_id,cluster_type,canonical_text,category,subcategory,evidence_count,source_diversity,demand_score,competition_score,pain_severity,commercial_intent,audit_score,confidence,validation_status,embedding_status,metadata,updated_at) values('taxonomy',${entityId},${clusterType},${text},${category},${subcategory},${idx.length},${domains.size},${result.market?.demand_score??null},${result.market?.competition_score??null},${severity},${intent},${audit},${conf},${status},${status==='validated'?'pending':'stale'},${sql.json({evidence_indices:idx,evidence_domains:[...domains],rationale:c.rationale||null,ai_verdict:verdict,worker:'semantic_category_pain_v3',retrieval_version:'greek_alias_v3+authoritative_context_v1',metric_semantics:{demand:'derived_evidence_density_proxy',competition:'derived_relevant_commercial_domain_proxy'},taxonomy_policy:'closed_semantic_product_taxonomy_v1'})},now()) on conflict(entity_type,entity_id,cluster_type,md5(canonical_text)) do update set category=excluded.category,subcategory=excluded.subcategory,evidence_count=excluded.evidence_count,source_diversity=excluded.source_diversity,demand_score=excluded.demand_score,competition_score=excluded.competition_score,pain_severity=excluded.pain_severity,commercial_intent=excluded.commercial_intent,audit_score=excluded.audit_score,confidence=excluded.confidence,validation_status=excluded.validation_status,embedding_status=case when evidence.semantic_clusters.embedding_status='ready' and excluded.validation_status='validated' then 'ready' else excluded.embedding_status end,metadata=evidence.semantic_clusters.metadata||excluded.metadata,updated_at=now()`;total++;if(status==='validated'){validated++;painScores.push(severity)}}const demand=result.market?.demand_score??null,comp=result.market?.competition_score===0?null:(result.market?.competition_score??null),pain=painScores.length?painScores.reduce((a,b)=>a+b,0)/painScores.length:null,opportunity=demand!==null&&comp!==null&&pain!==null?clamp(Number(demand)*.35+Number(pain)*.40+(100-Number(comp))*.25):null;await sql`insert into intel.category_market_snapshots(taxonomy_id,demand_score,competition_score,pain_gap_score,satisfaction_score,opportunity_score,confidence,demand_evidence,competition_evidence,pain_evidence,metadata) values(${entityId},${demand},${comp},${pain},null,${opportunity},${clamp(Number(result.market?.confidence??.5),0,1)},${sql.json(result.market?.demand_evidence||[])},${sql.json(result.market?.competition_evidence||[])},${sql.json(result.market?.pain_evidence||[])},${sql.json({source:'semantic_category_pain_v3',retrieval_version:'greek_alias_v3+authoritative_context_v1',query_aliases:result.market?.query_aliases||[],context_evidence:result.market?.context_evidence||[],metric_semantics:{demand:'derived_evidence_density_proxy_without_macro_inflation',competition:'derived_relevant_commercial_domain_proxy',pain:'ai_audited_evidence_cluster',context:'official_or_industry_exogenous_context'},validated_clusters:validated,ai_audit_summary:result.market?.ai_audit_summary||null,rejected_patterns:result.market?.rejected_patterns||[]})})`;await sql`update ops.collection_jobs set status='completed',completed_at=now(),lease_owner=null,lease_expires_at=null,last_error=null where id=${jobId} and entity_id=${entityId}`;return{observations:persisted,clusters:total,validated_clusters:validated,category,subcategory}}
Deno.serve(async req=>{if(req.method==='GET')return json({ok:true,service:'evidence-gateway',version:'3.0',collection_type:'semantic_category_pain_v2',retrieval:'greek_natural_alias_v3+authoritative_context_v1',deepseek_configured:Boolean(DEEPSEEK_KEY),model:MODEL,db_max_connections_per_instance:1});if(req.method!=='POST')return json({error:'method_not_allowed'},405);try{await auth(req);const b=await req.json(),action=String(b.action||'');if(action==='seed')return json({ok:true,...await seed()});if(action==='claim')return json({ok:true,jobs:await claim(Number(b.limit||20),String(b.worker||'semantic-category-pain-v3'))});if(action==='audit_batch')return json({ok:true,...await deepseekBatch(Array.isArray(b.items)?b.items:[])});if(action==='save')return json({ok:true,result:await save(b.result||{})});if(action==='fail'){await sql`update ops.collection_jobs set status=case when attempt_count>=4 then 'failed' else 'queued' end,last_error=${String(b.error||'worker_failed').slice(0,1200)},lease_owner=null,lease_expires_at=null,not_before=now()+interval '20 minutes' where id=${String(b.job_id||'')}`;return json({ok:true})}throw new Error('action_not_allowed')}catch(e){console.error(e);const msg=String(e instanceof Error?e.message:e);return json({error:msg},msg.includes('oidc')?401:500)}})
